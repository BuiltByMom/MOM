export type TProject = {
	title: string;
	subtitle?: string; // e.g., "WASH TRADE" or "WRAPPED"
	video?: string;
	image?: string;
	tags: string[];
	backgroundColor?: string; // For different background colors like black, blue, purple
	slug: string;
};
